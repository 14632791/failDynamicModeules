<Page x:Class="UpdateClient.Views.ResultPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="360" d:DesignWidth="600"
	Title="MainPage" Loaded="Page_Loaded">
    <Page.Resources>
        <Style x:Key="btnExpand" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}"></Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Center" Orientation="Vertical"  VerticalAlignment="Center">
            <Image Source="{StaticResource imagenoconnect}" Height="65" Margin="0,0,0,2" x:Name="imgFinish"/>
            <TextBlock Text="已更新" x:Name="_txtContent" HorizontalAlignment="Center" Margin="10" Foreground="{StaticResource TextBlockForeground}"/>

        </StackPanel>
        <Path Grid.Row="1" Style="{StaticResource LinePathStyle}" VerticalAlignment="Top"/>
        <Button  Grid.Row="1" Content="确 认" Style="{StaticResource CommonButtonStyle}" Click="btnConfirm_Click"  x:Name="btnConfirm"   Height="30" Width="80" Margin="5"   HorizontalAlignment="Center" >
        </Button>
    </Grid>
</Page>
